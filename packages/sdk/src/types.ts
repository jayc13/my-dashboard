/**
 * SDK types and interfaces
 *
 * Re-exports all types from the shared types package for convenience.
 */

// Re-export all types from the shared types package
export * from '@my-dashboard/types';
