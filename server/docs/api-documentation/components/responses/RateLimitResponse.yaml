description: Rate limit exeeded
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
        retryAfter:
          type: integer
          description: Time in seconds
      example:
        error: Too many failed attempts. Try again in 10 minutes.
        retryAfter: 600
    examples:
      Example 1:
        value:
          error: Too many failed attempts. Try again in 10 minutes.
          retryAfter: 600
