description: ''
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
      x-examples:
        Example 1:
          error: 'Unauthorized: Invalid or missing API key'
    examples:
      Internal server error:
        value:
          error: Internal server error
