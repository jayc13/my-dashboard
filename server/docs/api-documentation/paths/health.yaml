get:
  operationId: health-check
  summary: Health check
  description: Indicator of the server being alive
  tags:
    - Health Check
  responses:
    '200':
      description: Service is healthy
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: string
  security: []
